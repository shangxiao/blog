
                           Logging in Django


        • Incremental config doesn't apply here as the config
          is processed at-once during setup()

        • It's possible to define your own logging config schema
          by setting LOGGING_CONFIG - for example if you wanted
          to read the config from a yaml file 🤷

